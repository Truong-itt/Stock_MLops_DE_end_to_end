x-airflow-common: &airflow-common
  build:
    context: ./airflow
  user: "0:0"
  environment:
    AIRFLOW__CORE__EXECUTOR: LocalExecutor
    AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
    AIRFLOW__CORE__LOAD_EXAMPLES: "false"
    AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION: "true"
    AIRFLOW__WEBSERVER__SECRET_KEY: "airflow-student-secret"
    AIRFLOW__API__AUTH_BACKENDS: "airflow.api.auth.backend.basic_auth"
    MLFLOW_TRACKING_URI: http://mlflow:5000
    MODEL_NAME: churn_prediction_model
    PYTHONPATH: /opt/project
    MLFLOW_S3_ENDPOINT_URL: http://minio:9000
    AWS_ACCESS_KEY_ID: minio
    AWS_SECRET_ACCESS_KEY: minio123
    AWS_DEFAULT_REGION: us-east-1
    STOCK_SYSTEM_ROOT: /opt/project/Stock_system
    SYMBOL_REGISTRY_PATH: /app/config/symbol_registry.json
    TRANSFORMERS_CACHE: /app/.cache/huggingface
  volumes:
    - ./dags:/opt/airflow/dags
    - ./logs:/opt/airflow/logs
    - ../../../:/opt/project
    - ../../config:/app/config:ro
    - ../../data/log:/var/log/stockraw
    - ../../data/.cache/huggingface:/app/.cache/huggingface
  depends_on:
    postgres:
      condition: service_healthy
  networks:
  - stock-network
  
services:
  minio:
    image: minio/minio:RELEASE.2025-02-18T16-25-55Z
    container_name: student_minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minio
      MINIO_ROOT_PASSWORD: minio123
    ports:
      - "9100:9000"
      - "9101:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
    - stock-network

  minio-init:
    image: minio/mc:RELEASE.2025-02-15T10-36-16Z
    container_name: student_minio_init
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minio minio123;
      mc mb -p local/mlflow || true;
      mc anonymous set download local/mlflow || true;
      "
    depends_on:
      minio:
        condition: service_healthy
    networks:
    - stock-network

  postgres:
    image: postgres:16
    container_name: student_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5433:5432"
    networks:
    - stock-network
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  mlflow:
    build:
      context: ./mlflow
    container_name: student_mlflow
    command: >
      mlflow server
      --backend-store-uri sqlite:////mlflow/mlflow.db
      --serve-artifacts
      --artifacts-destination s3://mlflow
      --host 0.0.0.0
      --port 5000
    ports:
      - "5000:5000"
    volumes:
      - mlflow_data:/mlflow
    environment:
      MLFLOW_TRACKING_URI: http://mlflow:5000
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
      AWS_ACCESS_KEY_ID: minio
      AWS_SECRET_ACCESS_KEY: minio123
      AWS_DEFAULT_REGION: us-east-1
    depends_on:
      postgres:
        condition: service_healthy
      minio-init:
        condition: service_completed_successfully
    networks:
    - stock-network

  airflow-init:
    <<: *airflow-common
    container_name: student_airflow_init
    command: >
      bash -c "airflow db migrate &&
      airflow users create --username airflow --password airflow --firstname Student --lastname Project --role Admin --email airflow@example.com"
    networks:
    - stock-network

  airflow-webserver:
    <<: *airflow-common
    container_name: student_airflow_webserver
    command: webserver
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully
    networks:
    - stock-network

  airflow-scheduler:
    <<: *airflow-common
    container_name: student_airflow_scheduler
    command: scheduler
    depends_on:
      postgres:
        condition: service_healthy
      airflow-init:
        condition: service_completed_successfully
    networks:
    - stock-network

  # fastapi:
  #   build:
  #     context: ../../../serving_pipeline/simple_api
  #   container_name: student_fastapi
  #   environment:
  #     MLFLOW_TRACKING_URI: http://mlflow:5000
  #     MODEL_NAME: churn_prediction_model
  #     MODEL_ALIAS: production
  #     INFERENCE_LOG_PATH: /app/logs/inference_log.jsonl
  #     DRIFT_SUMMARY_PATH: /app/reports/drift_summary.json
  #     MLFLOW_S3_ENDPOINT_URL: http://minio:9000
  #     AWS_ACCESS_KEY_ID: minio
  #     AWS_SECRET_ACCESS_KEY: minio123
  #     AWS_DEFAULT_REGION: us-east-1
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ../../../serving_pipeline/simple_api/logs:/app/logs
  #     - ../../../serving_pipeline/simple_api/reports:/app/reports
  #   depends_on:
  #     - mlflow
  #   networks:
  #   - stock-network
    
  gradio:
    build:
      context: ./serving_pipeline/gradio_ui
    container_name: student_gradio
    environment:
      PREDICT_API_URL: http://whale-ml-service:8090/predict-event
      HEALTH_API_URL: http://whale-ml-service:8090/health
    ports:
      - "7860:7860"
    networks:
    - stock-network

  whale-ml-service:
    container_name: whale-ml-service
    build:
      context: ./whale_ml
    restart: unless-stopped
    environment:
      # db, store, mlflow
      
      CLICKHOUSE_HOST: clickhouse
      CLICKHOUSE_PORT: 8123
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: truongittstock
      CLICKHOUSE_DB: stock_warehouse
      SYMBOL_REGISTRY_PATH: /app/config/symbol_registry.json
      MODEL_ARTIFACT_PATH: /app/artifacts/whale_move_model.joblib
      MLFLOW_TRACKING_URI: http://mlflow:5000
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
      AWS_ACCESS_KEY_ID: minio
      AWS_SECRET_ACCESS_KEY: minio123
      AWS_DEFAULT_REGION: us-east-1
      WHALE_ML_MODEL_NAME: whale_move_forecaster
      WHALE_ML_MODEL_ALIAS: production
      # WHALE_ML_LOG_DIR: /var/log/whale_ml
      # WHALE_ML_LOG_FILE: whale_ml.log
      # WHALE_ML_LOG_KEEP_DAYS: "30"
      # WHALE_ML_LOG_TIMEZONE: Asia/Ho_Chi_Minh
      # PREFER_MLFLOW_REGISTRY: "1"
      # MLFLOW_REGISTRY_REQUIRED: "1"

      AUTO_TRAIN_ON_STARTUP: "1"
      # AUTO_RETRAIN_INTERVAL_MIN: "0"

      TRAIN_LOOKBACK_DAYS: "240"
      TRAIN_MAX_ROWS: "120000"
      TRAIN_MAX_EVENTS_PER_SYMBOL_DAY: "1"
      TRAIN_EVENT_SELECTION_STRATEGY: "strongest"
      WALK_FORWARD_ENABLED: "1"
      WALK_FORWARD_FOLDS: "4"
      WALK_FORWARD_MIN_TRAIN_DATES: "60"
      WALK_FORWARD_TEST_DATES: "20"
      RECENCY_WEIGHT_ENABLED: "1"
      RECENCY_WEIGHT_HALF_LIFE_DAYS: "10"
      RECENCY_WEIGHT_MIN: "0.25"
      DIRECTION_THRESHOLD_TUNING_ENABLED: "1"
      DIRECTION_THRESHOLD_MIN: "0.35"
      DIRECTION_THRESHOLD_MAX: "0.65"
      DIRECTION_THRESHOLD_STEP: "0.01"
      DIRECTION_THRESHOLD_METRIC: "balanced_accuracy"
      DIRECTION_RETURN_THRESHOLD: "0.0115"
      DIRECTION_NEUTRAL_POLICY: "drop"
      DIRECTION_LABEL_TARGET: "horizon_extreme"
      TRADE_FILTER_ENABLED: "1"
      TRADE_FILTER_DEFAULT_MIN_PROB_UP: "0.70"
      TRAIN_MIN_CP_PROB: "0.0"
      TRAIN_MIN_WHALE_SCORE: "0.0"
      TRAIN_MIN_INNOVATION_ABS: "0.0"
      MODEL_SELECTION_STD_PENALTY: "0.5"
      PREDICTION_AUDIT_ENABLED: "1"
      PREDICTION_AUDIT_TABLE: "whale_ml_prediction_audit"
      GLOBAL_PROMOTION_REQUIRE_BOTH: "1"
      GLOBAL_PROMOTION_MIN_DIRECTION_DELTA: "0.0"
      GLOBAL_PROMOTION_MIN_SESSIONS_DELTA: "0.0"
      MAX_FORECAST_HORIZON: "5"
      WHALE_ML_GLOBAL_CANDIDATE_ALIAS: "candidate"
      SYMBOL_TRAIN_COOLDOWN_MIN: "180"
      # SYMBOL_TRAIN_LOOKBACK_DAYS: "240"
      SYMBOL_TRAIN_MAX_ROWS: "20000"
      SYMBOL_MIN_TRAIN_SAMPLES: "350"
      SYMBOL_PROMOTION_REQUIRE_BOTH: "1"
      SYMBOL_PROMOTION_MIN_DIRECTION_DELTA: "0.0"
      SYMBOL_PROMOTION_MIN_SESSIONS_DELTA: "0.0"
      # SYMBOL_MLFLOW_REGISTRY_REQUIRED: "1"
      WHALE_ML_SYMBOL_CANDIDATE_ALIAS: "candidate"
    volumes:
      - ./whale_ml/artifacts:/app/artifacts
      - ./whale_ml/config:/app/config:ro
      - ./whale_ml/logs:/var/log/whale_ml
    ports:
      - "8090:8090"
    networks:
    - stock-network

volumes:
  minio_data:
  postgres_data:
  mlflow_data:

networks:
  stock-network:
    name: stock-network
    external: true
